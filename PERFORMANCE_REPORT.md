# 性能与压力测试报告

所有已记录运行均使用 Java 21。数据只证明表中明确列出的输入，不把启动时间、`Can't keep up` 或生成后的单点 TPS 偷换成区块吞吐。

## 长期压力测试（issue #473）

| 项目 | 值 |
|---|---|
| Forge | 47.4.10 |
| Java | Eclipse Temurin 21.0.12.1，`C:\Program Files\Eclipse Adoptium\jdk-21.0.12.101-hotspot` |
| 配置 | seed `0`，`-64..320`，`ore_fix=false` |
| 区块闭区间 | `[64,64]..[207,207]` |
| 规模 | 20,736 个新区块，81 个 tile |
| 生命周期 | 11 次服务端会话/重启；21 次保存；81 次解除强加载 |
| 运行时间 | 1,628.827 秒，包含命令屏障、保存和重启，折合整体 12.73 chunks/s |
| 结果 | 20,736 个 `chunk_full`；所有会话退出码 0；阻断日志 0 |
| 离线校验 | 20,736/20,736 个 full chunk，DataVersion 3465，完整 `-64..320` |
| 地形哈希 | `3931aa963f7b443a9bf74b709af503eb7c341425ad330de8155edd309445e4b2` |

证据 manifest：

`run/production-forge-47.4.10/batch-results/20260903-issue473-long-20736-forge47410/20260903-issue473-long-20736-forge47410-issue473-long-miny-m64-seed-0/manifest.json`

这关闭了计划要求的 20,000+ 区块长期崩溃门槛，但不是性能 A/B。运行器按逐区块 FULL 回执推进，所以整体时间包含协议、保存与 11 次 JVM 启动开销；没有可靠的单区块中位数、P95/P99 或 CPU 曲线。

## 功能场景观察

| 场景 | 规模 | 已测结果 | 不能据此声称 |
|---|---:|---|---|
| Region spline cache | 256 区块 | `Done 3.841s`，backlog `12.523s` | 没有缓存前后 matched A/B，不能给出提速百分比 |
| Overkill，832 高度 | 256 区块 | 出生区 `36.783s`，`Done 43.693s`，backlog `30.723s`；结束后 `3.729ms / 20 TPS` | 单点 heap `2,184,278K / 3,072,000K` 不是峰值或泄漏曲线 |
| `ore_fix`，`-128..320` | 256 区块 | `Done 16.443s`，backlog `8.321s`；结束前 `2.528ms / 20 TPS` | 不是 on/off 性能比较 |
| River Ice | 256 区块 | 正常保存停服，backlog `18.021s` | 不是吞吐基准 |
| River Lanterns | 256 区块 | `Done 3.971s`，backlog `13.078s` | 不是吞吐基准 |

矿物矩阵覆盖 50 个世界、12,800 个受控区块，用于分布正确性，不用于性能判断；每个 case 都曾出现一次 `Can't keep up`。完整数据在 `benchmarks/ore_fix/2026-09-03/`。

## 仍未通过的性能门禁

- 官方 3.0.17、社区默认、关闭新增功能、Overkill 和无 Tectonic 控制组之间的同机 matched A/B 尚未完成。
- 5 个固定种子、每个 4096 区块的候选矩阵尚未完成。
- 尚无可信的区块耗时中位数/P95/P99、CPU、峰值 RSS/heap、GC 暂停、JFR/Spark 热点或数据包 reload 时间。
- 客户端 10 次进出世界、专服内存增长曲线和线程转储门禁未完成。
- 严格地形确定性失败；即使性能数据后来达标，也不能据此绕过正确性阻断。

因此当前只能叫“候选构建”，不能叫性能稳定版。谁拿一条 `20 TPS` 就宣布优化成功，那属于拿仪表盘当论文，整岔劈了。
